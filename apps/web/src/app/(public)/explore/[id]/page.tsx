import { ExploreDetailClient } from './ExploreDetailClient';

export const metadata = {
  title: 'Project | VOJAS',
  description: 'MPLAD project transparency detail — finance, evidence, and risk signals.',
};

export default function ExploreDetailPage() {
  return <ExploreDetailClient />;
}
