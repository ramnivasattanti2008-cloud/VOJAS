import { AnalyticsSectorClient } from './AnalyticsSectorClient';

export const metadata = {
  title: 'Sector Analytics | VOJAS',
  description: 'MPLAD sector-level analytics — indicators, risk rules, and data sources.',
};

export default function AnalyticsSectorPage() {
  return <AnalyticsSectorClient />;
}
